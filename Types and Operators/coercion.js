/**
  * Coercion:
  * Converting a value from one type to another.
  * This happens quite often in JavaScript because it's
  * dynamically typed.
**/

// the number 1 gets coerced to string
var a = 1 + '2';
console.log(a);

//The javascript engine gets done it under the hood